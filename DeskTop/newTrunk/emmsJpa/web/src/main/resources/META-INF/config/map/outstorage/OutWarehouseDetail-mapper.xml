<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="OutWarehouseDetail">
    <resultMap id="OutWarehouseDetailMap" type="OutWarehouseDetail">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Mar 24 13:42:00 CST 2017.
        -->
        <id column="warehouse_detail_id" property="warehouseDetailId" jdbcType="VARCHAR"/>
        <result column="out_num" property="outNum" jdbcType="DECIMAL"/>
        <association property="pickNotice" column="pick_notice" select="PickNotice.selectByPk"/>
        <association property="pickNoticeDetail" column="pick_notice_detail" select="PickNoticeDetail.selectByPk"/>
        <association property="tallying" column="out_tallying" select="Tallying.selectByPk"/>
        <association property="materials" column="materials" select="Materials.selectByPk"/>
        <association property="wbs" column="wbs" select="Project.selectByPk"/>
        <association property="outWarehouse" column="outWarehouse" javaType="OutWarehouse" resultMap="OutWarehouse.OutWarehouseMap">
        </association>
            <association property="storagelocation" column="storagelocation" select="Storagelocation.selectByPk"/>
    </resultMap>
    <!-- 根据领料通知删除领料通知明细 -->
    <delete id="deleteOutWarehouseDetail" parameterType="string">
        delete from out_warehouse_detail owd
        <where>
            owd.warehouse_detail_id = #{outWarehouse.outWarehouseId}
        </where>
    </delete>

    <select id="queryDetailByOutWarehouse" resultMap="OutWarehouseDetailMap" parameterType="string">
        SELECT owd.* from out_warehouse_detail owd
        <where>
            owd.out_warehouse = #{outWarehouseId}
        </where>
    </select>
    <!-- 批量出库单明细 -->
    <insert id="insertList" parameterType="java.util.List" useGeneratedKeys="false">
        insert into out_warehouse_detail
        (warehouse_detail_id,out_warehouse,materials,wbs,pick_notice,pick_notice_detail,out_num,out_tallying,storagelocation)
        SELECT A.*
        FROM(
        <foreach collection="list" item="item" index="index" separator="UNION ALL">
            (SELECT
            #{item.warehouseDetailId,jdbcType=VARCHAR} warehouse_detail_id,
            #{item.outWarehouse.outWarehouseId,jdbcType=VARCHAR} out_warehouse,
            #{item.materials.materialsId,jdbcType=CHAR} materials,
            #{item.wbs.projectId,jdbcType=CHAR} wbs,
            #{item.pickNotice.pickId,jdbcType=CHAR} pick_notice,
            #{item.pickNoticeDetail.pickDetailId,jdbcType=CHAR} pick_notice_detail,
            #{item.outNum,jdbcType=VARCHAR} out_num,
            #{item.tallying.tallyingId,jdbcType=VARCHAR} out_tallying,
            #{item.storagelocation.storagelocationId,jdbcType=VARCHAR} storagelocation
            FROM dual)
        </foreach>
        )A
    </insert>
</mapper>